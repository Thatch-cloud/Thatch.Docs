# SDKs

> [!NOTE]
> **To be written.**

<!-- TODO: which languages ship first, where the packages live, and their versioning
     relationship to the API. Until an SDK exists, say so plainly here rather than
     leaving the page ambiguous. -->

Until a first-party SDK is published, use the HTTP API directly — see
[API overview](overview.md).
