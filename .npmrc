# The design system is published to GitHub Packages, not the public npm registry.
# Auth is per-developer: `gh auth token` (needs read:packages) or a PAT in ~/.npmrc.
# CI does not use this file for auth — actions/setup-node writes the token itself.
@thatch-cloud:registry=https://npm.pkg.github.com
